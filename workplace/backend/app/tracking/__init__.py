"""Source-tracking package (Stage 7): feed parsing + (later) feed resolution,
dedup, polling, and digest assembly. Tracking polls **operator-given** sources
only — it never discovers sources by topic (§2.2)."""
